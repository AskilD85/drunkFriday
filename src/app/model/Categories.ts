export class Categories {
    id: string;
    title: string;
    name: string;
    body: Text;

    constructor(id: string, title: string, name: string, body: Text) {
        this.id = id;
        this.title = title;
        this.name = name;
        this.body = body;

    }
}
